package com.nhlstenden.vote;

public abstract class Vote
{
    // A method isValid() should be created here to determine if a subclass of vote is a valid vote or not....
    // Determine the correct class where to implement that function.
    public abstract boolean isValid();
}
