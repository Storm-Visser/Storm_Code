
/**
 * Write a description of class BootTocht here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class BoatTrip
{
    private int getHoursFromString(String time, int index)
    {
        String[] timeParts = time.split(":");//0=hh, 1=mm
        if
        return Integer.parseInt(timeParts[0]);
    }
}
